import NumberInput from "../components/NumberInput";

import { useRef } from "react";

interface Props {
    submitTarget: string;
    enabled: boolean;
}
export default function Withdraw({ submitTarget }: Props){
    const formRef = useRef<HTMLFormElement>(null);

    return (

        <div>
            <h1>withdraw</h1>
          
                

         </div>

        
        );

}